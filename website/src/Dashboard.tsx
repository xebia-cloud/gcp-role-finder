import {Card, CardContent, CardHeader} from "@mui/material";

export const Dashboard = () => (
    <Card>
        <CardHeader title="Google Cloud Platform IAM Role finder"/>
        <CardContent>
            Fulltext search on the Google Cloud Platform IAM roles in your project and organization, intended
            to help you find the least privileged role for your actors.
        </CardContent>
    </Card>
);
